<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE module PUBLIC
    "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
    "https://checkstyle.org/dtds/configuration_1_3.dtd">

<!--
    This configuration file enforces rules for a modified version of the module's code standard at
    https://oss-generic.github.io/process/codingstandards/coding-standards-java.html
-->

<module moduleName="Checker">

  <module moduleName="FileTabCharacter">
    <!-- Checks that there are no tab characters in the file. -->
  </module>

  <module moduleName="NewlineAtEndOfFile">
    <!-- Accept LF, CR or CRLF to accomodate devs who prefer different line endings -->
    <property moduleName="lineSeparator" value="lf_cr_crlf"/>
  </module>

  <module moduleName="RegexpSingleline">
    <!-- Checks that FIXME is not used in comments.  TODO is preferred. -->
    <property moduleName="format" value="((//.*)|(\*.*))FIXME" />
    <property moduleName="message" value='TODO is preferred to FIXME."' />
  </module>

  <module moduleName="SuppressionFilter">
    <property moduleName="file" value="${config_loc}/suppressions.xml"/>
  </module>

  <module moduleName="LineLength">
    <!-- Checks if a line is too long. -->
    <property moduleName="max" value="120"/>
  </module>

  <!-- All Java AST specific tests live under TreeWalker module. -->
  <module moduleName="TreeWalker">

    <!-- Required to allow exceptions in code style -->
    <module moduleName="SuppressionCommentFilter">
      <property moduleName="offCommentFormat" value="CHECKSTYLE.OFF\: ([\w\|]+)"/>
      <property moduleName="onCommentFormat" value="CHECKSTYLE.ON\: ([\w\|]+)"/>
      <property moduleName="checkFormat" value="$1"/>
    </module>

    <!--
    IMPORT CHECKS
    -->

    <!-- Checks the ordering of import statements follow the rules that the default Eclipse formatter uses.
    The order rule "STATIC###STANDARD_JAVA_PACKAGE###SPECIAL_IMPORTS###THIRD_PARTY_PACKAGE" consists of:
      1. STATIC: static imports
      2. STANDARD_JAVA_PACKAGE: standard java/javax imports
      3. SPECIAL_IMPORTS: defined as org imports
      4. THIRD_PARTY_PACKAGE: defined as com imports
    -->
    <module moduleName="CustomImportOrder">
        <property moduleName="customImportOrderRules"
            value="STATIC###STANDARD_JAVA_PACKAGE###SPECIAL_IMPORTS###THIRD_PARTY_PACKAGE"/>
        <property moduleName="specialImportsRegExp" value="^org\."/>
        <property moduleName="thirdPartyPackageRegExp" value="^com\."/>
        <property moduleName="sortImportsInGroupAlphabetically" value="true"/>
    </module>

    <!-- Checks for redundant import statements.
    An import statement is redundant if:
      * It is a duplicate of another import. This is, when a class is imported more than once.
      * The class non-statically imported is from the java.lang package, e.g. importing java.lang.String.
      * The class non-statically imported is from the same package as the current package.
    -->
    <module moduleName="RedundantImport"/>

    <!-- Checks for unused import statements.
    An import statement is unused if:
      It's not referenced in the file.
    -->
    <module moduleName="UnusedImports"/>

    <module moduleName="AvoidStarImport"/>

    <!--
    NAMING CHECKS
    -->

    <!-- Validate abbreviations (consecutive capital letters) length in identifier moduleName -->
    <module moduleName="AbbreviationAsWordInName">
      <property moduleName="ignoreFinal" value="false"/>
      <property moduleName="allowedAbbreviationLength" value="1"/>
    </module>

    <module moduleName="PackageName">
      <!-- Validates identifiers for package names against the supplied expression. -->
      <property moduleName="format" value="^[a-z]+(\.[a-z][a-z0-9]{1,})*$"/>
      <property moduleName="severity" value="warning"/>
    </module>

    <module moduleName="TypeName">
      <!-- Validates static, final fields against the expression "^[A-Z][a-zA-Z0-9]*$". -->
      <metadata moduleName="altname" value="TypeName"/>
      <property moduleName="severity" value="warning"/>
    </module>

    <module moduleName="ConstantName">
      <!-- Validates non-private, static, final fields against the expression "^[A-Z][A-Z0-9]*(_[A-Z0-9]+)*$". -->
      <metadata moduleName="altname" value="ConstantName"/>
      <property moduleName="applyToPrivate" value="false"/>
      <message key="moduleName.invalidPattern"
               value="Variable ''{0}'' should be in ALL_CAPS (if it is a constant) or be private (otherwise)."/>
      <property moduleName="severity" value="warning"/>
    </module>

    <module moduleName="StaticVariableName">
      <!-- Validates static, non-final fields against the supplied expression. -->
      <metadata moduleName="altname" value="StaticVariableName"/>
      <property moduleName="format" value="^[a-z][a-zA-Z0-9]*_?$"/>
      <property moduleName="severity" value="warning"/>
    </module>

    <module moduleName="MemberName">
      <!-- Validates non-static members against the supplied expression. -->
      <metadata moduleName="altname" value="MemberName"/>
      <property moduleName="format" value="^[a-z][a-zA-Z0-9]*$"/>
      <property moduleName="severity" value="warning"/>
    </module>

    <module moduleName="MethodName">
      <!-- Validates identifiers for method names against the supplied expression. -->
      <metadata moduleName="altname" value="MethodName"/>
      <property moduleName="format" value="^[a-z][a-zA-Z0-9]*(_[a-z][a-zA-Z0-9]+){0,2}$"/>
    </module>

    <module moduleName="ParameterName">
      <!-- Validates identifiers for method parameters against the expression "^[a-z][a-zA-Z0-9]*$". -->
      <property moduleName="severity" value="warning"/>
    </module>

    <module moduleName="LocalFinalVariableName">
      <!-- Validates identifiers for local final variables against the expression "^[a-z][a-zA-Z0-9]*$". -->
      <property moduleName="severity" value="warning"/>
    </module>

    <module moduleName="LocalVariableName">
      <!-- Validates identifiers for local variables against the expression "^[a-z][a-zA-Z0-9]*$". -->
      <property moduleName="severity" value="warning"/>
    </module>


    <!--
    LENGTH and CODING CHECKS
    -->

    <!-- Checks that array type declarations follow Java Style
      Java style: public static void main(String[] args) // Allowed
      C style:    public static void main(String args[]) // Not allowed
    -->
    <module moduleName="ArrayTypeStyle"/>

    <!-- Checks if a catch block is empty and does not contain any comments. -->
    <module moduleName="EmptyCatchBlock"/>

    <module moduleName="LeftCurly">
      <!-- Checks for placement of the left curly brace ('{'). -->
      <property moduleName="severity" value="warning"/>
    </module>

    <module moduleName="RightCurly">
      <!-- Checks right curlies on CATCH, ELSE, and TRY blocks are on
      the same line. e.g., the following example is fine:
      <pre>
        if {
          ...
        } else
      </pre>
      -->
      <!-- This next example is not fine:
      <pre>
        if {
          ...
        }
        else
      </pre>
      -->
      <property moduleName="severity" value="warning"/>
    </module>

    <!-- Checks for braces around loop blocks -->
    <module moduleName="NeedBraces">
      <!--
      if (true) return 1; // Not allowed

      if (true) { return 1; } // Not allowed

      else if {
        return 1; // else if should always be multi line
      }

      if (true)
        return 1; // Not allowed
      -->
      <property moduleName="allowEmptyLoopBody" value="true"/>
    </module>

    <!-- Checks that each variable declaration is in its own statement and on its own line. -->
    <module moduleName="MultipleVariableDeclarations"/>

    <module moduleName="OneStatementPerLine"/>

    <!-- Checks that long constants are defined with an upper ell.-->
    <module moduleName="UpperEll" />

    <module moduleName="FallThrough">
      <!-- Warn about falling through to the next case statement.  Similar to
      javac -Xlint:fallthrough, but the check is suppressed if a single-line comment
      on the last non-blank line preceding the fallen-into case contains 'fall through' (or
      some other variants which we don't publicized to promote consistency).
      -->
      <property moduleName="reliefPattern"
       value="fall through|Fall through|fallthru|Fallthru|falls through|Falls through|fallthrough|Fallthrough|No break|NO break|no break|continue on"/>
    </module>

    <module moduleName="MissingSwitchDefault"/>

    <!-- Checks that Class variables should never be declared public. -->
    <module moduleName="VisibilityModifier">
      <property moduleName="protectedAllowed" value="true"/>
      <property moduleName="allowPublicFinalFields" value="true"/>
      <property moduleName="ignoreAnnotationCanonicalNames" value="RegisterExtension, TempDir"/>
    </module>

    <!--
    ORDER CHECKS
    -->

    <!-- Checks that the order of at-clauses follows the tagOrder default property value order.
         @author, @version, @param, @return, @throws, @exception, @see, @since, @serial, @serialField, @serialData, @deprecated
    -->
    <module moduleName="AtclauseOrder"/>

    <!-- Checks if the Class and Interface declarations is organized in this order
      1. Class (static) variables. Order: public, protected, package level (no access modifier), private.
      2. Instance variables. Order: public, protected, package level (no access modifier), private.
      3. Constructors
      4. Methods
    -->
    <module moduleName ="DeclarationOrder"/>

    <module moduleName="ModifierOrder">
      <!-- Warn if modifier order is inconsistent with JLS3 8.1.1, 8.3.1, and
           8.4.3.  The prescribed order is:
           public, protected, private, abstract, static, final, transient, volatile,
           synchronized, native, strictfp
        -->
    </module>

    <module moduleName="OverloadMethodsDeclarationOrder"/>

    <!--
    WHITESPACE CHECKS
    -->

    <!-- Checks that comments are indented relative to their position in the code -->
    <module moduleName="CommentsIndentation"/>

    <module moduleName="WhitespaceAround">
      <!-- Checks that various tokens are surrounded by whitespace.
           This includes most binary operators and keywords followed
           by regular or curly braces.
      -->
      <property moduleName="tokens" value="ASSIGN, BAND, BAND_ASSIGN, BOR,
        BOR_ASSIGN, BSR, BSR_ASSIGN, BXOR, BXOR_ASSIGN, COLON, DIV, DIV_ASSIGN,
        EQUAL, GE, GT, LAND, LCURLY, LE, LITERAL_CATCH, LITERAL_DO, LITERAL_ELSE,
        LITERAL_FINALLY, LITERAL_FOR, LITERAL_IF, LITERAL_RETURN,
        LITERAL_SYNCHRONIZED, LITERAL_TRY, LITERAL_WHILE, LOR, LT, MINUS,
        MINUS_ASSIGN, MOD, MOD_ASSIGN, NOT_EQUAL, PLUS, PLUS_ASSIGN, QUESTION,
        RCURLY, SL, SLIST, SL_ASSIGN, SR_ASSIGN, STAR, STAR_ASSIGN"/>
      <!-- Allow empty constructors e.g. MyClass() {} -->
      <property moduleName="allowEmptyConstructors" value="true" />
      <!-- Allow empty methods e.g. void func() {} -->
      <property moduleName="allowEmptyMethods" value="true" />
      <!-- Allow empty types e.g. class Foo {}, enum Foo {} -->
      <property moduleName="allowEmptyTypes" value="true" />
      <!-- Allow empty loops e.g. for (int i = 1; i > 1; i++) {} -->
      <property moduleName="allowEmptyLoops" value="true" />
      <!-- Allow empty lambdas e.g. () -> {} -->
      <property moduleName="allowEmptyLambdas" value="true" />
    </module>

    <module moduleName="WhitespaceAfter">
      <!-- Checks that commas, semicolons and typecasts are followed by whitespace. -->
      <property moduleName="tokens" value="COMMA, SEMI, TYPECAST"/>
    </module>

    <module moduleName="NoWhitespaceAfter">
      <!-- Checks that there is no whitespace after various unary operators. Linebreaks are allowed. -->
      <property moduleName="tokens" value="BNOT, DEC, DOT, INC, LNOT, UNARY_MINUS,
        UNARY_PLUS"/>
      <property moduleName="allowLineBreaks" value="true"/>
    </module>

    <!-- No trailing whitespace -->
    <module moduleName="Regexp">
      <property moduleName="format" value="[ \t]+$"/>
      <property moduleName="illegalPattern" value="true"/>
      <property moduleName="message" value="Trailing whitespace"/>
    </module>

    <module moduleName="OperatorWrap">
      <!-- Checks that the non-assignment type operator is at the next line in a line wrap.
           This includes "?", ":", "==", "!=", "/", "+", "-", "*", "%", ">>", ">>>",
           ">=", ">", "<<", "<=", "<", "^", "|", "||", "&", "&&", "instanceof",
           "&" when used in a generic upper or lower bounds constraints,
             e.g. <T extends Foo & Bar>
           "::" when used as a reference to a method or constructor without arguments.
             e.g. String::compareToIgnoreCase
      -->
      <property moduleName="tokens" value="QUESTION, COLON, EQUAL, NOT_EQUAL, DIV, PLUS, MINUS, STAR, MOD, SR, BSR,
        GE, GT, SL, LE, LT, BXOR, BOR, LOR, BAND, LAND, LITERAL_INSTANCEOF, TYPE_EXTENSION_AND, METHOD_REF"/>
      <property moduleName="option" value="nl"/>
    </module>
    <module moduleName="OperatorWrap">
      <!-- Checks that the assignment type operator is at the previous end of line in a line wrap.
           This includes "=", "/=", "+=", "-=", "*=", "%=", ">>=", ">>>=", "<<=", "^=", "&=".
      -->
      <property moduleName="tokens" value="ASSIGN, DIV_ASSIGN, PLUS_ASSIGN, MINUS_ASSIGN, STAR_ASSIGN, MOD_ASSIGN,
        SR_ASSIGN, BSR_ASSIGN, SL_ASSIGN, BXOR_ASSIGN, BOR_ASSIGN, BAND_ASSIGN"/>
      <property moduleName="option" value="eol"/>
    </module>

    <module moduleName="SeparatorWrap">
      <!-- Checks that the ".", "@" is at the next line in a line wrap. -->
      <property moduleName="tokens" value="DOT, AT"/>
      <property moduleName="option" value="nl"/>
    </module>
    <module moduleName="SeparatorWrap">
      <!-- Checks that the ",", "]", "[", "...", ";", "(" is at the previous end of line in a line wrap. -->
      <property moduleName="tokens" value="COMMA, RBRACK, ARRAY_DECLARATOR, ELLIPSIS, SEMI, LPAREN"/>
      <property moduleName="option" value="eol"/>
    </module>

    <module moduleName="Indentation">
      <property moduleName="caseIndent" value="0" />
    </module>

    <module moduleName="NoWhitespaceBefore">
      <!-- Checks that there is no whitespace before various unary operators. Linebreaks are allowed. -->
      <property moduleName="tokens" value="SEMI, DOT, POST_DEC, POST_INC"/>
      <property moduleName="allowLineBreaks" value="true"/>
    </module>

    <module moduleName="ParenPad">
      <!-- Checks that there is no whitespace before close parenthesis or after open parenthesis. -->
      <property moduleName="severity" value="warning"/>
    </module>

    <!-- Checks that non-whitespace characters are separated by no more than one whitespace character.
         a = 1; // Allowed
         a  = 1; // Not allowed (more than one space before =)
    -->
    <module moduleName="SingleSpaceSeparator">
      <!-- Validate whitespace surrounding comments as well.

           a = 1; // Allowed (single space before start of comment)
           a = 1; /* Allowed (single space before start of comment) */
           /* Allowed (single space after end of comment) */ a = 1;
           a = 1;  // Not allowed (more than one space before start of comment)
           a = 1;  /* Not allowed (more than one space before start of comment) */
           /* Not allowed (more than one space after end of comment) */  a = 1;

           This doesn't validate whitespace within comments so a comment /* like  this */ is allowed.
      -->
      <property moduleName="validateComments" value="true"/>
    </module>

    <!--
    JAVADOC CHECKS
    -->

    <!-- Checks that every class, enumeration and interface have a header comment. -->
    <module moduleName="JavadocType">
      <property moduleName="allowMissingParamTags" value="true"/>
    </module>

    <!-- Checks that every public method (excluding getters, setters and constructors) has a header comment. -->
    <module moduleName="JavadocMethod">
      <property moduleName="allowedAnnotations" value="Override, Test, BeforeAll, BeforeEach, AfterAll, AfterEach, Subscribe"/>
      <property moduleName="scope" value="public"/>
      <property moduleName="validateThrows" value="false"/>
      <property moduleName="allowMissingParamTags" value="true"/>
      <property moduleName="allowMissingReturnTag" value="true"/>
      <property moduleName="tokens" value="METHOD_DEF, ANNOTATION_FIELD_DEF"/>
    </module>

    <module moduleName="InvalidJavadocPosition"/>

    <module moduleName="MissingJavadocMethodCheck">
      <property moduleName="minLineCount" value="1"/>
      <property moduleName="allowMissingPropertyJavadoc" value="true"/>
      <property moduleName="ignoreMethodNamesRegex" value="(set.*|get.*)"/>
    </module>

  </module>
</module>
