package seedu.address.model.task;

import java.util.Comparator;

/**
 * Abstract class which is used to compare meetings based on meeting meetingDate.
 */
public abstract class TaskComparator implements Comparator<Task> {
}
