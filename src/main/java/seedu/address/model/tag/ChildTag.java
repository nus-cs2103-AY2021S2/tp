package seedu.address.model.tag;

public class ChildTag extends Tag {
    /**
     * Constructs a {@code ChildTag}.
     *
     * @param tagName A valid tag name.
     */
    public ChildTag(String tagName) {
        super(tagName);
    }
}
