package seedu.address.testutil;

import seedu.address.commons.core.identifier.Identifier;

/**
 * A utility class containing a list of {@code Identifier} objects to be used in tests.
 */
public class TypicalIdentifiers {
    public static final Identifier IDENTIFIER_FIRST_PERSON = Identifier.fromIdentifier(1);
    public static final Identifier IDENTIFIER_SECOND_PERSON = Identifier.fromIdentifier(2);
    public static final Identifier IDENTIFIER_THIRD_PERSON = Identifier.fromIdentifier(3);
}
