package seedu.address.testutil;

import seedu.address.commons.core.index.Index;

/**
 * A utility class containing a list of {@code Index} objects to be used in tests.
 */
public class TypicalIndexes {
    public static final Index INDEX_FIRST_PASSENGER = Index.fromOneBased(1);
    public static final Index INDEX_SECOND_PASSENGER = Index.fromOneBased(2);
    public static final Index INDEX_THIRD_PASSENGER = Index.fromOneBased(3);
}
